const express = require("express");
const router = express.router();

const { protect } = require("../middleware/authMiddleware");

// ADD PROTECTION FOR RESERVATIONS MAYBE?

module.exports = router;
